# Calcit 0.15.3 语法检查 / Calcit 0.15.3 syntax checks

## 中文

- 将 Calcit 与 `@calcit/procs` 对齐到 0.15.3，并升级已发布的 Phlox 0.7.9。
- 使用 `surface-latest-v2` 检查完整 Snapshot，并应用编译器能够证明安全的语法优化。
- 删除 `calcit.cirru` 的 generated 标记，使源码 diff 可见。

## English

- Align Calcit and `@calcit/procs` on 0.15.3 and upgrade to the published Phlox 0.7.9.
- Check the full Snapshot with `surface-latest-v2` and apply compiler-proven syntax cleanup.
- Remove the generated marker from `calcit.cirru` so source diffs remain visible.

## 验证 / Verification

- strict dependency and toolchain verification
- Calcit formatting, preprocessing, type and dynamic-method analysis
- JavaScript generation and Vite production build
